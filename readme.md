initialize node  npm init
create pulic file>temp>.gitkeep

create src > app.js constant.js index.js (New-Item (cmd))

create .evv file
create .env.sample
create .gitignore - goto gitignore genrator and generate node 

# install nodemon
npm i D nodemon